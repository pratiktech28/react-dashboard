"use client";
import { Bell, Moon, Search } from "lucide-react"; import { useTheme } from "next-themes"; import { initials } from "@/lib/utils";
export function Navbar({ user }: { user: { name?: string | null; email?: string | null; image?: string | null } }) { const { setTheme } = useTheme(); return <header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b bg-white/80 px-4 backdrop-blur dark:bg-slate-900/80 md:px-8"><div className="relative hidden sm:block"><Search className="absolute left-3 top-2.5 text-slate-400" size={16}/><input aria-label="Search dashboard" placeholder="Search" className="rounded-lg bg-slate-100 py-2 pl-9 pr-4 text-sm dark:bg-slate-800" /></div><div className="ml-auto flex items-center gap-3"><button aria-label="Toggle theme" onClick={()=>setTheme("dark")} className="rounded-lg p-2 hover:bg-slate-100"><Moon size={18}/></button><button aria-label="Notifications" className="rounded-lg p-2 hover:bg-slate-100"><Bell size={18}/></button>{user.image?<img src={user.image} alt="Profile" className="h-8 w-8 rounded-full"/>:<span className="grid h-8 w-8 place-items-center rounded-full bg-indigo-600 text-xs text-white">{initials(user.name)}</span>}</div></header>; }
