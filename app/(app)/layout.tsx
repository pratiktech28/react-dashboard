import { auth } from "@/auth"; import { redirect } from "next/navigation"; import { Sidebar } from "@/components/sidebar"; import { Navbar } from "@/components/navbar"; import { PageTransition } from "@/components/page-transition";
export default async function PrivateLayout({ children }: { children: React.ReactNode }) { const session = await auth(); if (!session?.user?.id) redirect("/login"); return <div className="app-shell min-h-screen md:grid md:grid-cols-[260px_1fr]"><Sidebar /><main className="min-w-0"><Navbar user={session.user}/><div className="mx-auto max-w-7xl p-4 pb-10 md:p-8"><PageTransition>{children}</PageTransition></div></main></div>; }
